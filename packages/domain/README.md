# `@monad-backend/domain`

Shared domain types for the FOSS Supabase Cloud-like control plane.

This package contains stable business concepts used by services, CLI, admin
UI, and SDK packages. It intentionally avoids database-specific snake_case
column names and runtime implementation details.
