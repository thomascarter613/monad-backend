# `@open-backend-cloud/events`

Shared event names and event envelope types for platform activity.

Events should be treated as durable platform facts. WP-0003 defines the
initial event vocabulary used by audit logging, deployments, provisioning,
backups, restores, quotas, and runtime health checks.
