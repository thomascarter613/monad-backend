# Supabase Edge Functions

This directory holds runtime-plane Edge Functions.

WP-0002 includes `runtime-health` as a minimal function proving that functions have a stable source-controlled home. Future work packets will add deployment tracking, function versioning, secrets handling, logs, and rollback workflows.
